variant: fcos
version: 1.5.0
systemd:
  units:
    - name: var-opt-movies.mount
      enabled: true
      contents: | 
        [Unit]
        Requires=network-online.target
        [Mount]
        What=truenas.home.0n5.us:/mnt/tank0/movies
        Where=/var/opt/movies
        Type=nfs4
        [Install]
        RequiredBy=docker.service
        WantedBy=multi-user.target
