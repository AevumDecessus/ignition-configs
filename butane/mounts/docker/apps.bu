variant: fcos
version: 1.5.0
systemd:
  units:
    - name: var-opt-docker.mount
      enabled: true
      contents: |
        [Unit]
        Requires=network.target
        [Mount]
        What=truenas.home.0n5.us:/mnt/fastdata/docker/apps
        Where=/var/opt/docker
        Type=nfs4
        [Install]
        RequiredBy=docker.service
        WantedBy=multi-user.target
