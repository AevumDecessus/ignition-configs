variant: fcos
version: 1.5.0
systemd:
  units:
    - name: var-opt-audiobooks-unsorted.mount
      enabled: true
      contents: | 
        [Unit]
        Requires=network.target
        [Mount]
        What=truenas.home.0n5.us:/mnt/tank0/audiobooks-unsorted
        Where=/var/opt/audiobooks-unsorted
        Type=nfs4
        [Install]
        RequiredBy=docker.service
        WantedBy=multi-user.target
