variant: fcos
version: 1.5.0
systemd:
  units:
    - name: var-opt-camera_old.mount
      enabled: true
      contents: | 
        [Unit]
        Requires=network-online.target
        [Mount]
        What=truenas.home.0n5.us:/mnt/tank0/camera
        Where=/var/opt/camera_old
        Type=nfs4
        [Install]
        RequiredBy=docker.service
        WantedBy=multi-user.target
